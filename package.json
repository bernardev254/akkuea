{
  "name": "scaffold-stellar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.0.25",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "bun run dev --parallel",
    "build": "bun run build",
    "start": "bun run start",
    "lint": "bun run lint",
    "test": "bun test",
    "clean": "bun run clean --parallel",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=20.11.0",
    "bun": ">=1.0.25"
  },
  "devDependencies": {
    "@types/node": "^24.5.1",
    "bun-types": "^1.0.25",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "brace-expansion": "^4.0.1",
    "husky": "^9.1.7",
    "next": "^15.5.3",
    "next-themes": "^0.4.6"
  }
}
