# Database Configuration
DB_HOST=postgres
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=akkuea
DB_PORT=5432

# API Configuration
PORT=8080

# JWT Configuration (if needed)
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION=24h

# Logging
LOG_LEVEL=info
